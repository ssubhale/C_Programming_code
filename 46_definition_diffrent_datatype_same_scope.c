#include <stdio.h>

int main(void)
{
    int No = 10;
    // float No = 20.00f;               // error C2374: 'No' : redefinition; multiple initialization

    printf("%d", No);                   // 10

    return 0;
}

